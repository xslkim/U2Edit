# QA 报告 — T0.7 中文 IME 输入验证

> 测试日期: 2026-04-16  
> 测试环境: Windows（自动化 + 真窗口建议补测）  
> 应用版本: commit 5369499

## 测试结果汇总

| # | 用例描述 | 结果 | 备注 |
|---|---------|------|------|
| 1 | 在普通 `<input>` 聚焦下用微软拼音输入「你好」，候选显示时按 Delete 不增加「画布删除计数」 | ⚠️ 待人工 | 需 `pnpm tauri dev` + 微软拼音实机验证 |
| 2 | Konva Text 双击内联编辑，输入中文，Enter 确认后画布文字正确 | ⚠️ 待人工 | 实现含 `compositionstart/end` 与 Enter 提交 |
| 3 | 选字空格不误判为「空格+左键画布平移」 | ✅ Pass（设计） | 本 POC 未将空格绑定平移（界面已说明） |

## 自动化门禁

- `pnpm run typecheck`：✅ Pass  
- `pnpm test`（Vitest）：✅ Pass  

## 代码审查要点（辅助）

- 全局 `keydown`（capture）中：IME **组合输入**（`compositionstart`/`end`）期间不触发删除/撤销计数。
- 焦点在 `input`/`textarea` 时不拦截，由浏览器处理文本编辑。

## 总结

- **最终结论**: **Pass（带人工补测项）** — 用例 1–2 必须在真实桌面窗口与微软拼音下复验；用例 3 由 POC 范围保证。
