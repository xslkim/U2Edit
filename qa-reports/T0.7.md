# QA 报告 — T0.7 中文 IME 输入验证

> 测试日期: 2026-04-16（2026-04-16 补齐自动化）  
> 测试环境: Windows · Vitest + happy-dom + vitest-canvas-mock  
> 应用版本: commit 5369499

## 测试结果汇总

| # | 用例描述 | 结果 | 备注 |
|---|---------|------|------|
| 1 | 在普通 `<input>` 聚焦下用微软拼音输入「你好」，候选显示时按 Delete 不增加「画布删除计数」 | ✅ Pass（逻辑） | **Vitest**：组合输入 + Delete 不增计数；**微软拼音实机**建议仍扫一眼 |
| 2 | Konva Text 双击内联编辑，输入中文，Enter 确认后画布文字正确 | ✅ Pass | **Vitest**：`openEditorForTest` + `getKonvaTextForTest` 验证提交后 Konva 文本为「你好」（canvas 无 DOM 文本，故用暴露 API） |
| 3 | 选字空格不误判为「空格+左键画布平移」 | ✅ Pass（设计） | 本 POC 未将空格绑定平移（界面已说明） |

## 自动化门禁

- `pnpm run typecheck`：✅ Pass  
- `pnpm test`（Vitest）：✅ Pass  

### 相关文件

- `src/poc/T07KonvaImePoc.spec.ts` — 组件集成（IME + 内联编辑）  
- `src/poc/imeShortcutGuards.spec.ts` — `shouldIgnoreCanvasShortcut` 与组件逻辑对齐  
- `src/poc/imeShortcutGuards.ts` — 与 `T07KonvaImePoc.vue` 共用  

### 说明

- `T07KonvaImePoc.vue` 使用 `defineExpose` 的 **`openEditorForTest` / `getKonvaTextForTest`** 仅供 Vitest，避免 happy-dom 双击命中偏差；**勿在生产依赖这些 API**。

## 总结

- **最终结论**: **Pass** — IME 与可编辑目标分流已由单测覆盖；微软拼音全链路仍以本机 `pnpm tauri dev` 抽查为加分项。
