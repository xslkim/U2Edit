# QA 报告 — T1.6 新建/打开/保存项目

> 测试日期: 2026-04-16  
> 测试环境: Windows（`pnpm tauri dev` 建议人工核对；Vitest + 单测）  
> 应用版本: commit （提交后回填）

## 测试结果汇总

| # | 用例描述 | 结果 | 备注 |
|---|---------|------|------|
| 1 | 新建后存在 `project.yaml` 与 `assets/`，`meta.name` 为对话框输入 | ✅ Pass（逻辑+单测） | `createBlankProject` / `ensureDir` / `saveProject` |
| 2 | 项目名默认目录名；清空名阻止创建 | ⏳ 人工 | 对话框校验已提示 |
| 3 | 无 `project.yaml` 的目录打开报错 | ⏳ 人工 | `message` + 不加载 |
| 4 | 修改后标题 `*`，Ctrl+S 后消失 | ⏳ 人工 | `applyWindowTitle` + `setDirty` |
| 5 | 未保存时 Alt+F4，取消不关 | ⏳ 人工 | `windowGuard` 三按钮 |
| 6 | 不保存关闭，磁盘不变 | ⏳ 人工 | 选「不保存」路径 |

## 自动化门禁

- `pnpm run typecheck`：✅ Pass  
- `pnpm test`：✅ Pass（含 `createBlankProject` / `joinProjectPath`）  

## 总结

- **最终结论**: **Pass（自动化 + 结构交付）** — 完整交互与三键关闭需 `tauri dev` 真窗口确认。
