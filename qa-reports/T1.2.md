# QA 报告 — T1.2 YAML 读写与校验

> 测试日期: 2026-04-16  
> 测试环境: Windows（Vitest + Node fs 读 fixture）  
> 应用版本: commit （见 status.md）

## 测试结果汇总

| # | 用例描述 | 结果 | 备注 |
|---|---------|------|------|
| 1 | 加载 requirements §3.2 示例 YAML，`validate` 无报错 | ✅ Pass | `src/core/fixtures/requirements-3.2-sample.yaml` |
| 2 | 重复 id，`validate` 含重复 id 信息 | ✅ Pass | Vitest |
| 3 | id=`123abc` 不通过 | ✅ Pass | `ID_FORMAT` |
| 4 | 嵌套 7 层不通过 | ✅ Pass | `DEPTH` |
| 5 | 不存在 assetId | ✅ Pass | `ASSET_MISSING` |
| 6 | stringify → parse 深度相等 | ✅ Pass | |
| 7 | nodes 空 / 双根 / 根非 panel | ✅ Pass | `ROOT_COUNT` / `ROOT_TYPE` |

## 自动化门禁

- `pnpm run typecheck`：✅ Pass  
- `pnpm test`：✅ Pass  

## 总结

- **最终结论**: **Pass** — `loadProject`/`saveProject` 基于既有 `fileService`（Tauri），核心逻辑由 `parseProjectYaml`/`stringifyProject`/`validate` 单测覆盖。
